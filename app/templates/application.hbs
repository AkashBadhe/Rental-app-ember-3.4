<div class="container">
  <div class="menu">
    {{#link-to "index"}}
      <h1>
        <em>
          Rental App
        </em>
      </h1>
    {{/link-to}}
    <div class="links">
      {{#link-to "about" class="menu-about"}}
        About
      {{/link-to}}
      {{#link-to "contact" class="menu-contact"}}
        Contact
      {{/link-to}}
      {{#link-to "agents" class="menu-agents"}}
        Agents
      {{/link-to}}
    </div>
  </div>
  <div class="body">
    {{outlet}}
  </div>
</div>